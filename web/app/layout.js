import './globals.css';

export const metadata = { title: 'Reward Hub — Megapot Rewards · Hence', description: 'Trading fees roll back into Megapot entries, minted to your own wallet' };

export default function RootLayout({ children }) {
  return (
    <html lang="en"><body>{children}</body></html>
  );
}

import './globals.css';

export const metadata = { title: 'Sealed Caller — Hence × Megapot × Inco', description: 'Sealed bonusball predictions on the $1M+ Megapot daily draw' };

export default function RootLayout({ children }) {
  return (
    <html lang="en"><body>{children}</body></html>
  );
}
